name: FlyBrain
options:
  bundleIdPrefix: com.snedea
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.2"
  generateEmptyDirectories: true

targets:
  FlyBrain:
    type: application
    platform: iOS
    deploymentTarget:
      iOS: "17.0"
    sources:
      - path: FlyBrain
        type: group
    settings:
      base:
        SWIFT_VERSION: "5.0"
        PRODUCT_BUNDLE_IDENTIFIER: com.snedea.flybrain
        IPHONEOS_DEPLOYMENT_TARGET: "17.0"
        INFOPLIST_FILE: FlyBrain/Info.plist
        GENERATE_INFOPLIST_FILE: false
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    postBuildScripts:
      - name: "Copy Web Assets"
        script: |
          set -euo pipefail
          WEB_ROOT="${SRCROOT}/.."
          DEST="${BUILT_PRODUCTS_DIR}/${CONTENTS_FOLDER_PATH}"

          # Copy index.html
          cp "${WEB_ROOT}/index.html" "${DEST}/index.html"

          # Copy css/
          mkdir -p "${DEST}/css"
          rsync -a --delete "${WEB_ROOT}/css/" "${DEST}/css/"

          # Copy js/ (includes js/vendor/)
          mkdir -p "${DEST}/js"
          rsync -a --delete "${WEB_ROOT}/js/" "${DEST}/js/"

          # Copy svg/
          mkdir -p "${DEST}/svg"
          rsync -a --delete "${WEB_ROOT}/svg/" "${DEST}/svg/"

          # Copy data/ (connectome binary, group metadata, the paired neuron
          # sidecar and neuron positions -- exclude large CSVs). Sidecar and
          # positions must match the binary; regenerate them with scripts/ if
          # it changes.
          mkdir -p "${DEST}/data"
          cp "${WEB_ROOT}/data/connectome.bin.gz" "${DEST}/data/connectome.bin.gz"
          cp "${WEB_ROOT}/data/neuron_meta.json" "${DEST}/data/neuron_meta.json"
          cp "${WEB_ROOT}/data/neuron_sidecar.bin.gz" "${DEST}/data/neuron_sidecar.bin.gz"
          cp "${WEB_ROOT}/data/neuron_sidecar.json" "${DEST}/data/neuron_sidecar.json"
          cp "${WEB_ROOT}/data/neuron_positions.bin.gz" "${DEST}/data/neuron_positions.bin.gz"
          cp "${WEB_ROOT}/data/neuron_positions.json" "${DEST}/data/neuron_positions.json"

          echo "Web assets copied to ${DEST}"
        basedOnDependencyAnalysis: false
        inputFiles: []
        outputFiles: []
